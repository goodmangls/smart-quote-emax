E-MAX Worldwide Express — Smart Quote System
============================================

안녕하세요, <%= @user.name || @user.email %> 님 / Hello, <%= @user.name || @user.email %>

아래 링크를 클릭하시면 로그인하실 수 있습니다.
Click the link below to log in:

<%= @magic_url %>

이 링크는 <%= @expires_in %> 동안 유효하며 1회만 사용 가능합니다.
This link is valid for <%= @expires_in %> and can only be used once.

본인이 요청하지 않은 경우 이 이메일을 무시하세요.
If you did not request this, please ignore this email.
