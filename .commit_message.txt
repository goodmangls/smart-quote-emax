♻️ refactor(api): `:unprocessable_entity` → `:unprocessable_content` 전환 (34곳) — Rack 3.2.6 은 이미 `SYMBOL_TO_STATUS_CODE[:unprocessable_entity]` 를 **`nil` 로 비웠고** 호환 shim 으로만 422 를 돌려준다. Rack 이 shim 을 걷어내면 422 를 쓰는 전 경로가 깨진다. **버그 수정이 아니라 마감 전에 shim 의존을 끊는 작업**이고 wire 포맷은 그대로다. ⚠️**검증 설계가 핵심**: 컨트롤러와 스펙을 같은 커밋에서 바꾸면 "둘이 서로 합의했다"만 증명된다 → **컨트롤러 26곳만 먼저 바꾸고 스펙은 손대지 않은 채 rspec 319건 전부 통과**시켜 wire 동일성을 입증한 뒤 스펙 8곳을 정리했다. 발견 2건: ①**경고는 컨트롤러가 아니라 rspec matcher 가 내고 있었다**(컨트롤러 변경 후에도 8건 그대로 → 스펙까지 바꾸자 0). 컨트롤러 쪽은 조용히 shim 에 의존 중이었다. ②**422 를 숫자로 고정한 스펙이 한 곳도 없었다** — 계약이 심볼 이름에만 걸려 있었다. wire 가드 1건 추가(`response.status).to eq(422)`), 400 주입으로 첫 RED 확인. reason phrase 는 코드에서 나오므로 불변. 범위: app 26 + spec 8 + 현행 설계문서 1. **`docs/archive/**` 2곳은 이력이라 보존.** rspec 320/0, rubocop 141 files 0 offenses, deprecation 경고 8→0, 프론트 변경 0. main 과 동일 전환(같은 스택이라 한쪽 발견이 그대로 적용됨).
