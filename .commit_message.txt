🔌 feat(fsc): 계산기가 Admin 위젯(DB) 요율을 읽도록 — FE·BE 양쪽. emax 는 `fsc_rates` 테이블·`FscFetcher`·`fsc_controller`·Admin `FscRateWidget` 이 전부 있는데도 **`FscFetcher` 가 컨트롤러에서만 쓰이고 계산 경로는 참조조차 하지 않아, 관리자가 위젯에서 요율을 바꿔도 견적에 아무 영향이 없었다**(smart-quote-main 은 최소한 BE 가 DB 우선이었으나 emax 는 양쪽 다 상수). **BE**: `QuoteCalculator#default_fsc_for` 가 DB 우선으로, 상수는 미매핑 캐리어·DB 조회 실패 시 폴백(FscFetcher 가 이미 DEFAULT_RATES 반환). 호출이 계산당 여러 번이라 메모이제이션, `FDX`→`FEDEX` 별칭 매핑. **FE**: `useCarrierFscDefault` 훅 신설로 하드코딩 3곳 통합 — 캐리어 전환 효과·최적캐리어 후보(이제 화면과 같은 요율로 최저가를 고름)·비교 페이지. `resolve(carrier)` 로 임의 캐리어를 풀고, `onApply` 를 생략하면 동기화 자체를 건너뛴다(비교 페이지는 편집 필드가 없어 resolve 만 필요). ⚠️**EMAX 는 per-kg FSC**(`EMAX_FSC_PER_KG`)라 별도 분기 — DB 에 EMAX 행이 생겨도 퍼센트를 절대 안 쓴다(FE·BE 양쪽 테스트로 고정). ⚠️사용자 입력 요율은 DB 응답이 늦게 와도 안 덮인다(마지막 적용값 ref 추적 + 초기값 시드로 응답 전 입력까지 보호). `===` 비교라 명시적 0 도 존중. **결함주입 2종 증명** — BE DB조회 제거 시 5건 RED, FE DB무시 시 3건 RED. 📝 문서: 매주 바뀌는 FSC 수치를 CLAUDE.md 에 박아둔 2곳이 2026-07-20 값으로 stale 해 있었다(40.5/38.5/39.75) → 파일 참조로 전환. FE 428 green(420→+8)·BE 319 green(310→+9), tsc·lint·build·rubocop clean
