💱 기준환율 1350→1320 갱신 (2026-09-02 기준, 입력값 그대로 적용) — **같은 KRW 견적의 USD 표시가 +2.3% 오른다.** 파트너에게 나가는 USD 금액이 바로 움직이는 값이라 배포 전 인지가 필요하다. 📐 **정책이 바뀌었다(2026-09-02)**: 종전 `floor(송금환율 / 50) × 50` 50원 내림을 폐지하고 **입력값을 그대로 적용**한다. 구 정책은 내림으로 안전 마진을 자동 부여했지만(환율을 낮게 잡으면 USD 견적이 높아지고 실제 시장 환율로 환전 시 KRW 가 더 들어온다) **마진 크기가 그 주 시장이 50원 경계 어디에 떨어지느냐에 따라 1원~49원으로 들쭉날쭉했다**(1401→1400 이면 버퍼 1원). 이제 마진 판단은 숫자를 입력하는 사람에게 있다 — ⚠️**입력값이 곧 적용 환율이고 곧 마진이다.** 1320 이 송금환율 그 자체라면 버퍼는 0 이다. 🏷️ **출처 주석을 정정했다**: `# 하나은행 월요일 09시 송금환율 (날짜)` → `# 적용 기준환율 (2026-09-02 확인)`. 새 정책에서 이 숫자는 사용자가 준 값이지 **하나은행 월요일 09시 고시라는 근거가 없고**, 2026-09-02 는 수요일이라 "월요일" 이라는 표기 자체가 사실과 어긋났다. 확인할 수 없는 출처를 모든 견적이 의존하는 상수에 박아두는 것은 값이 틀린 것 다음으로 나쁘다. 🔧 스킬(`~/.claude/skills/fx-update/`)도 함께 개선했다(저장소 밖이라 이 커밋에는 안 들어간다): 위치 인자(`fx-apply.py 1320`) 지원, 50원 단위 제약 제거, **주석 전체를 재작성**(값·날짜만 갈아끼우던 구조라 라벨이 낡은 채 남는 게 위 문제의 원인이었다), 읽기 정규식 완화. 구 정책은 `--market` 으로 남겨 명시할 때만 동작한다. 안전장치: **500~3000 밖 거부** · **5% 이상 변동 경고** · 적용 후 두 파일 재읽기 검증(앵커 불일치 시 exit 1). ⚠️**환율은 FSC 와 달리 DB·Admin 위젯이 없어 상수뿐이다 — 배포해야 반영된다.** TS↔RB 교차 검증 장치가 없어 한쪽만 고치면 조용히 어긋나므로 `--check` 로 두 파일 일치를 확인했다. 🔥 **이 저장소는 실제로 방치된 적이 있다** — 1450(2026-03-24)이 5개월간 남아 있다가 2026-08-25 에야 1350 으로 정렬됐고, 그때 같은 화물의 USD 표시가 +7.4% 움직였다. 그래서 스킬 기본값이 `--repo both` 다. 검증: tsc 0, eslint 0, vitest **40파일 472건**, rubocop 141파일 0 offense, rspec **327/0**(exit 0 직접 확인 — 파이프 뒤 tail 의 exit code 는 rspec 것이 아니라 별도로 돌렸다). smart-quote-main 도 **같은 값·같은 주석**으로 동시 처리했다.
