⏳ fix(fsc): 재조회 진행 중에도 옛 값을 "현재 DB" 로 단정하던 것 교정 + 테스트 격리 결함 제거 — **정직한 문구가 버튼을 누르는 순간 거짓 숫자로 되돌아갔다.** `useFscRates.load()` 는 시작하자마자 `setError(null)` 을 하고 성공할 때까지 `setData` 를 건드리지 않는다. 그래서 요청이 나가 있는 동안은 **에러 플래그 없이 저장 시도 이전 값을 들고 있는 상태**가 되고, 직전 커밋의 2분기(`ratesError ? 읽지못함 : 숫자`)는 그 창에서 옛 숫자를 자신 있게 `현재 DB:` 로 찍었다. 하필 관리자가 "읽지 못했습니다" 를 보고 **재조회 버튼을 누른 직후**가 정확히 이 창이라, 확인하려는 행동이 오히려 거짓 정보를 부르는 구조였다. 조치: `dbValueLabel()` 로 **3분기**화 — `loading` → `확인 중…`, `ratesError` → `읽지 못했습니다`, 그 외 → 숫자. 조회 중인 테이블에 대해 확정적 주장을 하지 않는다는 게 요지다. 🐛 **함께 잡은 테스트 격리 결함**: `vi.clearAllMocks()` 는 **호출 기록만 지우고 구현은 남긴다.** 새로 추가한 "loading 으로 뒤집는" 케이스의 `mockRetry.mockImplementation` 이 다음 케이스로 새어 `loading:true` 를 만들었고, 그러면 재조회 버튼이 `disabled` 라 클릭이 먹지 않아 **컴포넌트 버그처럼 보이는 실패**가 났다. `vi.resetAllMocks()` 로 교체하고 이유를 주석에 남겼다 — 케이스 순서에 따라 CI 에서만 깨지는 전형적인 형태다. 테스트 1건 추가(총 24건). **결함주입으로 판별력 실증**: `loading` 분기를 빼자 정확히 1건 RED. 검증: tsc 0, eslint 0, vitest **40파일 472건**(471 → +1). 📝 이로써 이 위젯의 "현재 DB" 표시는 **확정·미확정·조회실패·조회중** 네 상태를 모두 구분한다. 부분 실패 UI 에서 상태를 하나라도 뭉뚱그리면 그 지점이 그대로 거짓 정보가 된다는 걸 다섯 번 연속으로 확인했다.
