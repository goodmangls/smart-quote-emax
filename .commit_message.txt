🚨 fix(fsc): 저장 실패를 삼키던 `catch {}` 제거 — **일부 캐리어만 반영된 채 관리자가 성공으로 오인할 수 있었다.** 앞 커밋에서 main 의 `useFscRateEdit` 을 이식하며 `catch {}` 와 `// Error surfaced via the useFscRates error state` 주석을 그대로 가져왔는데 **그 주석이 거짓이다** — `useFscRates.error` 는 `GET /api/v1/fsc/rates` 실패만 담고 `POST /api/v1/fsc/update` 실패는 어디에도 반영되지 않는다. 캐리어당 별도 POST 라서 3번째(FEDEX)에서 터지면 UPS·DHL 은 **이미 DB 에 기록되고** FEDEX·OCS 는 옛 값으로 남는데, 화면에는 아무 메시지도 없고 `setIsEditing(false)` 도 `fetchRates()` 도 실행되지 않아 편집폼만 그대로 떠 있다. 관리자는 저장된 줄 알고 떠나고 **그 순간부터 캐리어마다 다른 주차의 요율로 견적이 나간다** — FSC 는 총액에 직접 곱해지는 값이라 조용한 금액 오차가 된다. 조치: ①`saveError` 상태 신설 + `role='alert'` 배너로 화면에 표시 ②**성공한 캐리어와 남은 캐리어를 이름으로 구분해 알린다**(`일부만 저장됐습니다 — UPS·DHL 는 새 요율, FEDEX·OCS 는 이전 요율입니다`) — "저장 실패" 만으로는 관리자가 테이블이 어느 상태인지 알 수 없어 복구 판단을 못 한다. 순차 저장이라 `written` 이 정확한 prefix 가 되는 점을 이용했다 ③`Sentry.captureException` 으로 원격 보고 ④**실패해도 `fetchRates()` 를 다시 돈다** — 관리자가 저장했다고 믿는 값이 아니라 테이블에 실제로 들어간 값을 보여줘야 한다(이 재조회는 자체 try/catch 로 감싸 미처리 rejection 을 막았다) ⑤편집 모드는 열어 둬서 부분 기록이 눈에 보이고 재제출이 가능하다 ⑥편집 시작·취소 시 `saveError` 초기화. 테스트 6건 추가(총 17건). **결함주입으로 판별력 실증**: 원래의 삼키는 `catch` 로 되돌리자 정확히 4건 RED. 검증: tsc 0, eslint 0, vitest **40파일 465건**(459 → +6). ⚠️**smart-quote-main 의 `useFscRateEdit.ts` 에 같은 결함이 그대로 있다** — 거기도 `catch {}` 에 같은 거짓 주석이고 캐리어 3개를 순차 POST 한다. 이 저장소만 고쳤다. 별건으로 처리할 것이며, 문서에 적어두는 것으로는 옮겨지지 않는다는 게 `mail` CVE 7일 방치에서 이미 확인된 사실이다.
