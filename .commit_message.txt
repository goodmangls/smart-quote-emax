⛽ UPS·DHL·FedEx FSC 2026-09-14 주간 요율 반영 (UPS 49.75% / DHL 43.75% / FedEx 49.0%) — 46.75/43.5/46.0 → 49.75/43.75/49.0. UPS·FedEx 가 각각 **+3.00** 로 크게 올랐다. **이 커밋은 견적 금액을 바꾸지 않는다.** 적용 요율의 출처는 DB(`fsc_rates`)=Admin 위젯이고, 여기 4파일(`rates.ts` · `rates.rb` · `fsc-history.ts` · `fsc-history.test.ts`)은 **DB 조회 실패·요청 대기 중 폴백**이자 이력 차트 시드다. 프로덕션 DB 는 프로브 시점에 아직 46.75/43.50/46.00 이었다 — **위젯 입력이 선행돼야 견적이 움직인다.** ✅**DHL 43.75% 는 공식 페이지 파싱과 교차 확인했다**(`9월 14-20, 2026` 밴드, 사용자 제공값과 일치). 다음 주 45.00% 도 이미 게시돼 있다. UPS·FedEx 는 봇 차단이라 사용자 제공값이다. ✅**DB 현재값은 프로덕션 응답으로 역산했다** — `marginPercent:0`·`fscPercent` 생략 호출의 `intlFsc/intlBase`. ⚠️**대조군으로 명시 `fscPercent:50` 을 함께 쏴 정확히 50.00% 를 확인**했다 — 프로브가 요율을 재고 있다는 증거가 없으면 숫자가 맞아 보여도 의미가 없다. ⚠️**OCS 는 건드리지 않았다** — ad-hoc 주기라 주간 갱신 대상이 아니고 25.0%(2026-07-20) 그대로다. ⚠️**EMAX 캐리어는 per-kg FSC**(`EMAX_FSC_PER_KG`)라 이 퍼센트 경로를 아예 쓰지 않는다. 🔴**이 저장소에는 smart-quote-main 의 스냅샷 드리프트 게이트가 없다** — `shared/tariff-snapshots/` 도 `tariff:snapshot` rake task 도 없어서 TS↔RB 가 어긋나도 잡아주는 장치가 없다. 이번엔 `fsc-apply.py` 가 두 파일을 같은 값으로 함께 써서 안전하지만, 손으로 고칠 때는 직접 대조해야 한다. (main 에서는 오늘 그 게이트가 첫 실전 작동을 했다 — 스냅샷 재생성 전 BE·FE 양쪽 RED.) 검증: tsc 0, eslint 0, vitest **472/472**, rubocop 141파일 0 offense, rspec **327/0** — 전부 green. smart-quote-main 도 **같은 값·같은 날짜**로 동시 처리했다. ⚠️두 저장소의 vitest 를 동시에 돌리지 않고 순차로 실행했다.
