🔍 fix(fsc): 재조회가 실패해도 옛 값을 "현재 DB" 로 단정하던 것 교정 + 죽은 try/catch 제거 — **가장 흔한 경로에서 거짓말을 했다.** 직전 커밋이 실패 시 각 입력칸 아래 `현재 DB: 33.33%` 를 노출했는데, 그 값의 출처인 `data` 는 **재조회가 실패하면 저장 시도 이전 읽기 그대로**다. `useFscRates.load()` 는 catch 에서 `setError` 만 하고 `setData` 를 건드리지 않기 때문이다. 그런데 POST 가 네트워크 문제로 죽었으면 뒤이은 GET 도 같은 이유로 죽을 가능성이 높다 — **하필 이 경로가 가장 흔하다.** 그 상태에서 "현재 DB" 라고 적으면, 관측한 적 없는 테이블 상태를 단정하는 것이고, 이 에러 경로가 존재하는 이유 자체(모르는 걸 안다고 하지 않기)를 배반한다. 함께 발견: **감싸 둔 try/catch 가 죽은 코드였다.** `fetchRates` 는 `useFscRates` 의 `retry` 이고 그 훅이 내부에서 catch 해 `error` 상태로만 흡수하므로 **절대 reject 하지 않는다** — 즉 그 catch 는 한 번도 실행될 수 없었고, "재조회 실패를 처리했다" 는 착각만 만들고 있었다. 조치: ①try/catch 제거하고 왜 필요 없는지(그리고 대신 무엇을 봐야 하는지) 주석으로 남김 ②위젯이 `useFscRates` 의 `error` 를 구독해, 조회 실패 시 숫자 대신 **`현재 DB: 읽지 못했습니다`** 를 표시 ③**편집 중 재조회 버튼 추가**(`saveError` 있을 때만) — 기존엔 편집 모드에서 새로고침 컨트롤이 숨겨져 있어 "현재 DB 를 확인하라" 는 안내를 따르려면 **편집을 취소해 입력값을 버리는 수밖에 없었다.** 입력은 보존되므로 재확인 후 그대로 재제출할 수 있다. 테스트 3건 추가(총 23건): 재조회 실패 시 옛 값(33.33)을 노출하지 않는다 · 재조회 버튼이 입력값을 유지한 채 다시 읽는다 · 실패 전에는 그 버튼이 없다. **결함주입으로 판별력 실증**: `ratesError` 를 무시하도록 되돌리자 정확히 1건 RED. 검증: tsc 0, eslint 0, vitest **40파일 471건**(468 → +3). 📝 이번 리뷰 4연속이 전부 같은 뿌리였다 — **한 커밋의 결정이 다른 커밋의 전제를 조용히 무너뜨림**(침묵 catch → throw=실패 가정 → 편집모드 유지가 안내를 무효화 → 재조회 실패가 표시를 무효화). 부분 실패 UI 는 "성공/실패" 2치가 아니라 **확정·미확정·미시도, 그리고 그걸 확인할 수단의 가용성**까지 설계해야 한다.
