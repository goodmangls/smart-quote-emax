🌍 카피(마케팅) 목적지 국가 수 190 → 220+ 로 정정, 근거를 실측치로 교체 — **처음엔 250+ 로 올리려 했으나 코드를 세어보니 어느 기준으로도 지지되지 않았다.** `src/config/` 를 직접 집계한 결과: `UPS_ZONE_MAP` 202 · `DHL_ZONE_MAP` 227 · `FEDEX_ZONE_MAP` 205 → **요율 합집합 237**, 그런데 `COUNTRY_OPTIONS`(드롭다운) 는 **191** 이라 방문자가 실제로 즉시 견적을 받을 수 있는 건 둘의 교집합 **190개국**이다. 즉 `250+` 는 요율 커버리지(237)도 넘고, **ISO 3166-1 전체 코드 수 249 보다도 큰 주장**이었다. UPS·DHL·FedEx 본인들이 공식적으로 쓰는 표현도 "220+ countries and territories" 다. 그래서 `220+` 로 정한다 — 캐리어 공식 표현과 일치하고, 우리 요율 커버리지(237)가 그보다 넓어 모순되지 않으며, 분쟁 시 근거가 명확하다. main 도 같은 수치로 맞춘다(현재 main 은 `250+`, 실측은 emax 와 동일한 237/192/191). 코드 8곳 + 문서: `index.html` 메타 3종(description·og·twitter — 검색결과에 그대로 노출된다) · `translations.ts` KO 3곳·EN 2곳 · `LandingPage.tsx` 통계 타일 2곳. ⚠️**`landing.stat.countries` 라벨에서는 숫자를 뺐다**(`190개 목적지 국가`→`목적지 국가`, `190 Destinations`→`Destinations`): 타일이 `value` 로 이미 숫자를 그리므로 라벨에도 넣으면 `220+` 위에 `220개 목적지 국가` 가 겹치고 `+` 유무까지 어긋난다. main 이 이미 이 구조라 맞춘 것으로, 단순 숫자 치환만 했다면 중복이 남았다. 📝 `copy.md` 정책 문단을 **실측 표로 교체**했다. ⚠️**이전 정책 문구는 근거 숫자 자체가 틀려 있었다** — "UPS·DHL·FedEx 합산 가능 목적지는 최대 약 265개국" 이라 적혀 있었으나 코드는 237 이고, 그 틀린 전제 위에 `250+` 가 세워져 있었다(main 에서 emax 로 그대로 복사될 뻔했다). 앞으로 숫자를 올릴 때는 표를 다시 세고 근거를 함께 적도록 명시했다. ⚠️**요율은 있는데 고를 수 없는 국가가 47개**(237−190, 대부분 소규모 속령·제재 대상국)라는 사실도 적어 뒀다 — "1초 만에 견적 완료" 가 실제로 성립하는 범위는 190개국이며, 카피를 더 올리려면 `COUNTRY_OPTIONS` 를 먼저 넓혀야 한다. 치환은 파일별 등장 횟수를 단언하는 스크립트로 수행(요율표의 `190200`·`1900` 같은 금액과 섞이지 않도록 `_tariff.ts`·`public/data` 제외). ⚠️소스가 아니라 **빌드 산출물로 검증**: `dist/index.html` 이 `전 세계 220+개국`, 번들에 `220+개국 국제 운임`·`220+ Countries, Instantly.`, `190`·`250` 계열 문구 0건. 검증: tsc 0, eslint 0, vitest 40파일 472건, build 성공. 테스트·e2e 가 이 문구에 의존하지 않음을 사전 확인했다.
