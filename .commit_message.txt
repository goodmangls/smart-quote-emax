⛽ UPS·DHL·FedEx FSC 2026-09-07 주간 요율 반영 (UPS 46.75% / DHL 43.5% / FedEx 46.0%) — 48.25/43.25/47.75 → 46.75/43.5/46.0. **이 커밋은 견적 금액을 바꾸지 않는다.** 적용 요율의 출처는 DB(`fsc_rates`)=Admin 위젯이고 그쪽은 이미 입력·검증을 마쳤다. 여기 4파일은 **DB 조회 실패·요청 대기 중 폴백**이자 이력 차트 시드다(`rates.ts` · `rates.rb` · `fsc-history.ts` · `fsc-history.test.ts`). ✅**DB 반영을 프로덕션 응답으로 검증했다** — 계산 API 를 `marginPercent:0`·`fscPercent` 생략으로 호출해 `intlFsc/intlBase` 로 역산했고 46.75/43.50/46.00 이 나왔다. ⚠️**대조군으로 명시 `fscPercent:50` 을 같이 쏴 정확히 50.00% 를 확인**했다 — 프로브가 요율을 재고 있다는 증거가 없으면 숫자가 맞아 보여도 의미가 없다. ✅DHL 은 공식 페이지가 값과 주차까지 확증(`9월 7-13, 2026 → 43.50%`). UPS·FedEx 는 봇 차단이라 사용자 제공값. ⚠️**OCS 는 건드리지 않았다** — ad-hoc 주기라 주간 갱신 대상이 아니고 25.0%(2026-07-20) 그대로다. ⚠️**EMAX 캐리어는 per-kg FSC**(`EMAX_FSC_PER_KG`)라 이 퍼센트 경로를 아예 쓰지 않는다. 검증: tsc 0, eslint 0, vitest **472/472**, rubocop 141파일 0 offense, rspec **327/0** — 전부 green. smart-quote-main 도 **같은 값·같은 날짜**로 동시 처리했다(그쪽 `RouteSection.test.tsx` 실패는 HEAD 반복 실행에서 재현된 **기존 비결정 결함**으로, 이 변경과 인과 경로가 없음을 확인했다). ⚠️두 저장소의 vitest 를 **동시에 돌리지 않고 순차로** 실행했다. ⚠️`rates.rb` 가 바뀌므로 Render 백엔드 배포 대상이다.
