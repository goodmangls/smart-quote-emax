📝 chore: CLAUDE.md backend 배포 가이드 monorepo 모드로 정정 — render.yaml `rootDir: smart-quote-api` 설정 반영, legacy `git subtree push --prefix=smart-quote-api api-deploy main` 지시 제거. 현재는 `git push origin main`만으로 Render가 smart-quote-api/ 변경 감지해 자동 재배포함. "separate smart-quote-api.git repo" 표현도 제거.
