🛡️ chore(deps): Rails 8.1.3.1 + `mail` 2.9.1 — **실제로 걸려 있던 CVE 를 7일 방치하고 있었다.** `bundler-audit` 이 `GHSA-mvxr-6m87-mv2q`(malformed RFC 2047 encoded-word 로 이메일 주소 스푸핑, Medium) 1건을 잡았다. main 은 2026-08-21 분석에서 같은 건을 발견해 그때 닫았고 그 문서에 **"smart-quote-emax 도 볼 것"이라고 적어 두기까지 했는데, 적어 둔 것만으로는 적용되지 않았다** — 이 저장소는 `mail 2.9.0` 그대로였다. emax 도 매직링크·견적서 메일을 보내므로 헤더 스푸핑은 무관하지 않다. `bundle update mail --conservative` 로 2.9.0→2.9.1, **그 젬 하나만 이동**. 감사 재실행 결과 `No vulnerabilities found`(1→0). 🔴 **교훈: 같은 스택 두 저장소는 문서에 적지 말고 감사를 두 번 돌릴 것.** 2026-08-11 에도 두 저장소의 고유 GHSA 집합이 완전히 동일했다 — 한쪽 발견은 다른 쪽에 그대로 적용되는데, 사람이 옮기는 단계에서 새는 게 반복된다. 함께 Rails 이관: **인용돼 온 "EOL 2026-10-07" 은 틀렸다** — `rubyonrails.org/maintenance` 1차 확인으로 8.0.x 는 **버그픽스가 2026-05-07 에 이미 종료**됐고 보안 종료는 **2026-11-07**(인용값은 월·일이 뒤바뀐 것으로 보인다). `~> 8.0.4` → `~> 8.1.3`, 락 8.0.5.1 → **8.1.3.1**(보안 2027-10-10 까지). ⚠️`config.load_defaults 8.0` 은 유지 — 젬 버전만 올리고 프레임워크 동작은 고정해 회귀면을 좁혔다. `--conservative` 라 rails 계열 13개 젬만 이동했고 **puma `~> 7.2.1` 핀은 무손상**(풀면 운영 웹서버가 메이저 점프한다). 검증: rspec **327/0**, rubocop 141파일 0 offense, bundler-audit 0건. main 과 **동일한 버전·동일한 방식**으로 맞췄다. ⚠️8.1.x 의 버그픽스 지원은 2026-10-10 까지 — 다음 시리즈가 나오면 두 저장소를 같이 올릴 것.
