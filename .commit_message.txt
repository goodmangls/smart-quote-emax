💱 기준환율 1320→1300 갱신 (2026-09-05 기준, 입력값 그대로 적용) — **같은 KRW 견적의 USD 표시가 +1.5% 오른다.** 파트너에게 나가는 USD 금액이 바로 움직이는 값이라 배포 전 인지가 필요하다. 2026-09-02 에 확정한 정책대로 **주어진 숫자를 반올림 없이 그대로** 적용했다(구 50원 내림 규칙은 `--market` 으로만 남아 있고 이번엔 쓰지 않았다). ⚠️**입력값이 곧 마진이다** — 안전 버퍼를 얼마나 담을지는 숫자를 부르는 쪽의 판단이고 스크립트는 아무것도 더하거나 빼지 않는다. 2파일(`src/config/rates.ts` · `smart-quote-api/lib/constants/rates.rb`)을 `fx-apply.py` 로 함께 수정하고 적용 후 재읽기로 검증했다. ⚠️**환율은 FSC 와 달리 DB·Admin 위젯이 없어 상수뿐이다 — 배포해야 반영된다.** TS↔RB 교차 검증 장치가 없어 한쪽만 고치면 조용히 어긋나므로 `--check` 로 네 파일 일치를 확인했다. 🔥 **이 저장소는 실제로 방치된 적이 있다** — 1450(2026-03-24)이 5개월간 남아 있다가 2026-08-25 에야 1350 으로 정렬됐고, 그때 같은 화물의 USD 표시가 +7.4% 움직였다. 그래서 스킬 기본값이 `--repo both` 이고 main 과 항상 같은 값을 쓴다. 검증: tsc 0, eslint 0, vitest **472건**, rubocop 141파일 0 offense, rspec **327/0**. ⚠️main 과 vitest 를 **동시에 돌리지 않고 순차로** 실행했다 — 병렬 시 무관한 테스트가 타임아웃으로 산발 실패하는 리소스 경합이 이 머신에서 확인됐다. smart-quote-main 도 **같은 값·같은 날짜**로 동시 처리했다.
