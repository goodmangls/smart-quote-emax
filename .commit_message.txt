🛡️ fix(api): 숫자 입력 범위 검증 이식 — main PR #83 의 `NUMERIC_INPUT_BOUNDS` 를 emax 로. **emax 백엔드에는 검증이 아예 없었다**(`validate_quote_input!` 도 `InvalidInputError` 도 부재). 2026-08-26 프로덕션 실측 4건 전부 200 통과: `fscPercent:9999` → 견적 17,774,300원(정상 88,000원의 **200배**) · `fscPercent:-50` → 원가 이하 · `exchangeRate:0` → **`totalQuoteAmountUSD:null` 인데 200**(KRW 는 정상으로 보여 완전 무음) · `exchangeRate:"abc"` → 동일(`"abc".to_f`=0.0 이라 범위검사만으로는 못 막는다, 그래서 `NUMERIC_INPUT_PATTERN` 이 있다). ⚠️**핵심 함정**: emax 는 `calculate`·`create` 끝에 `rescue StandardError` → 일반 `CALCULATION_ERROR`("Failed to calculate quote") 가 있어서, `InvalidInputError < StandardError` 를 그냥 두면 **어느 필드가 틀렸는지 사라진 채 422** 가 나간다. `rescue InvalidInputError` 를 그 **위에** 두고, 스펙도 status 가 아니라 **code·message 로 단언**했다(status 만 보면 기존 blanket rescue 와 구분 불가). 검증 순서: **스펙 먼저 작성 → 5건 RED 로 갭 실증 → 구현 → green → `fscPercent` 경계를 ±99,999 로 넓혀 정확히 2건 RED 확인 → 복원.** `create` 는 계산 전에 검증하도록 `QuoteCalculator.call` 앞에 배치. **`discountPercent` 는 의도적 제외** — 계산기가 `clamp(0, MAX_DISCOUNT_PERCENT)` 로 금액을 이미 묶는다(main 의 marginPercent 와 같은 이유). 그 스펙은 실계산기(`and_call_original`)로 돌린다 — 파일 최상단 스텁 때문에 처음 작성분이 스텁을 검사하고 있었다. FE 1파일 동반 수정: `dutyTaxEstimate` 입력칸에 clamp 부재로 음수 입력 시 새로 422 를 맞게 돼 있어, 같은 코드베이스의 `fscPercent` 패턴대로 0 하한을 걸었다. ⚠️**범위 밖(별건 판단 필요)**: `destinationCountry` 미검증 + UPS 존 `Z10 "Rest of World"` 폴백 — 오타 국가코드가 조용히 Rest-of-World 요율을 받는다. main 은 폴백을 제거했지만 emax 유지는 기록상 의도된 것이라 손대지 않았다. emax 에는 파트너 API 가 없어(`quote_api` 부재) 영향 범위는 JWT 인증 트래픽 한정. rspec 327/0, rubocop 141 files 0 offenses, FE 448/0, tsc·lint·build clean.
