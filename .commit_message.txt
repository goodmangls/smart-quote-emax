🔔 feat(dashboard): 기준환율 이탈 경고·리마인더 — smart-quote-main PR #94 를 emax 로 포팅. 대시보드 Real-time Exchange Rates 가 원달러를 상시 보고 있는데 `DEFAULT_EXCHANGE_RATE` 는 누가 `/fx-update` 를 돌려야만 움직여서 둘을 잇는 게 없었다. **이 저장소가 1450(2026-03-24)으로 5개월 방치돼 있던 걸 2026-08-25 에야 발견한 게 정확히 그 실패 모드**라, main 보다 여기가 더 필요한 기능이다. 정책(`floor(시장/50)×50`)으로 계산한 버킷이 적용값과 다르면 🔴재검토, 같은 버킷이지만 경계 15원 이내면 ⚠️근접을 위젯 상단에 띄운다. ⚠️**임계값 근거**: 위젯은 시장(중간)환율이고 정책 입력은 하나은행 송금환율이라 둘이 다르다 — 실측 1383.13 vs 1385(2원 미만)였지만 TT 스프레드가 1%(약 14원)까지 가는 경우를 감안해 15원 밴드로 잡았다. ⚠️**숫자를 번역 문자열 밖으로** 렌더한다(보간하면 번역이 없을 때 정작 실행에 필요한 숫자가 사라진다). ⚠️**위젯 테스트가 `DEFAULT_EXCHANGE_RATE` 실값에 묶이지 않도록 `vi.hoisted`+부분 mock 으로 상수를 고정**했다 — 안 그러면 다음 `/fx-update` 가 값을 바꾸는 주에 무관한 테스트가 깨진다(main 에도 같은 하드닝을 별도 PR 로 적용). mock 유효성은 1350→1400 주입으로 3건 RED 확인. 기존 픽스처 기본 환율 1385.5 는 근접 밴드 안이라 1370(조용)으로 낮췄다. 신규 테스트 20건(로직 16·위젯 4), FE 448 green, tsc·lint·build clean.
