⛽ UPS·DHL·FedEx FSC 2026-08-31 주간 요율 반영 (UPS 48.25% / DHL 43.25% / FedEx 47.75%) — 직전 주(2026-08-24) 대비 UPS +1.25pp · DHL +0.75pp · FedEx +1.25pp 로 세 캐리어 모두 인상. **DHL 은 공식 페이지에서 교차 검증했다** — `mydhl.express.dhl` 의 주간 밴드가 `8월 31-9월 6, 2026 → 43.25%` 로, 직전 밴드 42.50%·다음 밴드(9월 7-13) 43.50% 사이에 정확히 들어맞아 세 값이 같은 주차 세트임이 확인됐다. UPS·FedEx 는 봇 차단이라 페이지 확인 없이 사용자 제공값을 그대로 적용한다. ⚠️**이 커밋만으로는 견적 금액이 바뀌지 않는다** — 견적에 적용되는 요율의 단일 출처는 DB(`fsc_rates`) = Admin FSC 위젯이고(2026-08-24부터 FE `useCarrierFscDefault`·BE `QuoteCalculator#default_fsc_for` 가 DB 우선), 여기서 고친 상수는 **DB 조회 실패·요청 대기 중 폴백**이자 이력 차트 시드다. 위젯 입력은 별도로 완료했다. 4파일을 `fsc-apply.py` 로 한 번에 처리: `src/config/rates.ts` · `smart-quote-api/lib/constants/rates.rb` · `src/config/fsc-history.ts`(차트 시드 append) · `src/config/__tests__/fsc-history.test.ts`(의도적인 사람-확인 체크포인트 — 갱신 안 하면 RED). ⚠️**OCS 는 손대지 않았다** — ad-hoc 주기라 주간 갱신에 따라 움직이지 않으며 25.0% @ 2026-07-20 을 그대로 유지한다(가드 테스트가 이 값을 별도로 단언한다). **EMAX 캐리어는 per-kg FSC**(`EMAX_FSC_PER_KG`)라 애초에 퍼센트 경로가 아니다. 검증: tsc 0, eslint 0(`--max-warnings 0`), vitest **39파일 448건 전건 통과**, rubocop 141파일 0 offense, rspec **327/0**. smart-quote-main 과 **같은 값으로 동시 처리**했다 — 두 저장소는 같은 캐리어 공식 요율을 쓰므로 한쪽만 갱신하면 조용히 어긋난다.
