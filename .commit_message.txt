📝 CLAUDE.md·AGENTS.md 의 환율 정책 문장을 사실로 교정 — 두 파일 모두 "하나은행 09시 송금환율을 **50원 내림**(`floor(시장/50)×50`)"이라고 적혀 있었으나 **2026-09-02 에 정책이 바뀌어 부르는 숫자를 반올림 없이 그대로 적용**한다(`/fx-update 1300` → 1300). 구 규칙은 `--market` 플래그로만 남아 명시할 때만 동작한다. 폐기 이유를 함께 적었다 — 버퍼가 시장 위치에 따라 **1원~49원으로 들쭉날쭉**했고(1401 → 1400 이면 1원), 이제 ⚠️**입력값이 곧 마진**이라 스크립트는 아무것도 더하거나 빼지 않는다(송금환율을 그대로 넣으면 버퍼 0). `DEFAULT_EXCHANGE_RATE` 옆 괄호도 `(하나은행 월요일 09시 송금환율)` → `(적용 기준환율)` 로 고쳤다. ⚠️**이 저장소가 특히 위험하다** — 1450 이 5개월 방치된 전력이 있고, 환율은 FSC 와 달리 DB·Admin 위젯이 없어 상수뿐이라 배포해야 반영된다. smart-quote-main 의 두 파일도 **같은 문장을 갖고 있어 동시에 고쳤다**(스킬 기본값이 `--repo both` 인 것과 같은 이유). 검증: 4파일 전체에서 `50원 내림` 잔존 0건이고 구 정책은 이력 서술로 파일당 1건만 남는다. 코드 변경 없음 — `src/`·`smart-quote-api/` 무변경이다.
