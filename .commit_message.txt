🔌 fix(fsc): Admin 위젯에 요율 편집 UI 이식 — **emax 는 `fsc_rates` 를 쓰는 경로가 아예 없어 견적 FSC 가 최초 시드값에 묶여 있었다.** 오늘 주간 요율(48.25/43.25/47.75)을 반영하고 배포했는데도 화면이 이전 값 그대로라는 제보에서 출발했다. 원인 3중 결합: ①`FscRateWidget` 이 `rates.ts` 상수를 읽어 **표시만** 하는 읽기 전용이었다(`// rates.ts is the single source of truth for FSC (DB auto-apply disabled)`) ②`updateFscRate` 는 `src/api/fscApi.ts` 에 정의돼 있었지만 **호출부가 저장소 어디에도 없는 死코드**였다(main 은 `useFscRateEdit.ts` 가 호출한다) ③`FscRate.seed_carrier!` 는 `return if exists?` 라 **행이 한 번 생기면 상수를 바꿔도 절대 덮지 않는다**. 셋이 합쳐져, 2026-08-24(`85e6f66`)에 계산기가 DB 우선으로 바뀐 순간부터 **견적이 시드 시점 값에 동결됐고 매주 상수를 갱신·배포해도 금액이 1원도 움직이지 않았다.** 시드는 `ensure_defaults!` 가 들어온 2026-04-05(`1515e6d`) 이후 이뤄졌고 당시 상수는 UPS 46.25 · DHL 30.5 였다 — 실제 동결값은 프로덕션 확인 필요. 조치는 **프론트 전용**이다: 백엔드는 이미 완비돼 있었다(`POST /api/v1/fsc/update` + `require_admin!` + 감사로그, `SUPPORTED_CARRIERS` 에 OCS 포함, 라우트·요청스펙 모두 존재) — 그래서 백엔드는 **한 줄도 건드리지 않았다**. ⚠️`ensure_defaults!` 도 일부러 그대로 뒀다: 관리자가 값을 넣은 뒤에는 `return if exists?` 가 **오히려 그 편집을 보호**하는 올바른 동작이 되고, `FscFetcher.update!` 는 `find_or_initialize_by` + `update!` 라 덮어쓰기가 정상 작동한다. 갱신형으로 바꿨다면 조회할 때마다 관리자 편집이 날아갔을 것이다(`fsc_rate_spec.rb:41` 이 이 불변식을 이미 고정하고 있다). 신규 `fsc/useFscRateEdit.ts` — ⚠️**main 은 UPS/DHL/FEDEX 3캐리어 하드코딩이라 그대로 옮기면 OCS 가 조용히 저장되지 않는다.** emax 는 OCS 가 `SUPPORTED_CARRIERS` 에 있고 하필 ad-hoc 주기라 아무도 못 알아챌 캐리어여서 **4캐리어로 확장**했다. EMAX 는 per-kg(`EMAX_FSC_PER_KG`)라 편집 대상에서 의도적으로 제외 — `default_fsc_for` 가 EMAX 에 0 을 돌려주며 "행이 생겨도 퍼센트를 집지 말 것"이라 주석으로 못박아 둔 지점이다. 위젯은 소스를 상수→DB(`useFscRates`)로 바꾸고 연필/체크/취소/새로고침 헤더와 셀 입력 분기, `loading && !data` 스피너 경로를 추가했다(기존엔 로딩이 "Failed to load rates" 로 보였다). **2×2 레이아웃·EMAX 행·History 패널·`carrierLinks` 는 손대지 않았다** — 요청은 편집 경로지 리팩터가 아니다. 신규 테스트 11건, **결함주입 2종으로 판별력 실증**: ①데이터 소스를 상수로 되돌리자 4건 RED ②`handleSave` 를 main 처럼 3캐리어로 자르자 OCS 단언 1건 정확히 RED. ⚠️요율 mock 은 `rates.ts` 에 존재하지 않는 값(11.11/22.22/33.33/44.44)을 쓴다 — 실제 주간 요율을 쓰면 DB 를 읽든 상수를 읽든 통과해서 검사가 무력해지고, `/fsc-update` 가 도는 주마다 무관하게 깨진다. 검증: tsc 0, eslint 0, vitest **40파일 459건**(39/448 → +1파일 +11건, 새 `.tsx` 가 실제 수집됐는지 개수로 확인). 📝 문서: `CLAUDE.md`·`AGENTS.md` **쌍둥이 동시 수정** — "평시 주간 갱신은 위젯만으로 끝난다"가 emax 에서는 **거짓이었다**(main 문서를 복사해 둔 것). 이제 참이 됐고, 어긋나 있던 기간과 원인을 함께 적었다. ⚠️**이 커밋만으로 견적 금액이 고쳐지지 않는다** — 관리자가 `/admin` 에서 48.25/43.25/47.75 를 입력해야 한다. 배포 후 위젯은 동결된 옛 값을 보여줄 텐데 그게 회귀가 아니라 **진단 결과**다.
