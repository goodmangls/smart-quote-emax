🩺 fix(fsc): 저장 실패 안내가 응답 단절을 "미반영"으로 단정하던 것 교정 — **알 수 없는 것을 안다고 말하고 있었다.** 직전 커밋의 메시지는 캐리어를 `written`(저장됨) / 나머지(이전 요율) **두 갈래**로 갈랐는데, 요청이 throw 했다는 건 **클라이언트가 응답을 못 받았다는 뜻일 뿐**이다. 타임아웃·소켓 끊김·502 는 서버가 행을 이미 커밋한 뒤에도 발생한다 — 즉 실패한 그 캐리어는 **반영 여부 불확실**이지 "이전 요율"이 아니다. 관리자가 이 안내를 믿고 "FEDEX 는 안 들어갔구나" 하고 넘어가면, 실제로는 들어간 값을 모른 채 운영하게 된다. 상태는 **세 갈래**로 나뉜다: ①실패 이전 캐리어 = 성공 응답을 받았으므로 **저장 확정** ②실패한 캐리어 = **불확실**(서버 반영 여부 알 수 없음) ③그 뒤 캐리어 = 요청 자체를 안 보냈으므로 **이전 요율 확정**. 조치: 루프가 `inFlight` 를 비관적으로 들고 있다가(요청 직전 세팅, 성공 시 해제) throw 시 정확히 어느 캐리어가 미확인인지 특정한다. 메시지도 세 문장으로 분리하고, 불확실한 캐리어에 대해서는 단정 대신 **"아래 표시된 현재 값으로 확인해 주세요"** 로 실패 후 재조회한 화면을 가리킨다 — 우리가 알 수 없는 DB 상태를 주장하지 않는 게 유일하게 정직한 처리다. 테스트 3건을 이 구분에 맞게 다시 쓰고 1건 추가(총 18건): 특히 **"실패한 캐리어를 이전 요율이라고 말하지 않는다"** 를 부정 단언으로 고정했다 — 직전 커밋의 `변경된 캐리어는 없습니다` 단언이 바로 그 거짓 주장이었다. **결함주입으로 판별력 실증**: 실패 캐리어를 다시 "미시도" 로 뭉뚱그리자 정확히 3건 RED. 검증: tsc 0, eslint 0, vitest **40파일 466건**(465 → +1). 📝 교훈: 부분 실패 안내는 "무엇이 성공했나" 만큼 **"무엇을 모르는가"** 를 말해야 한다. 네트워크 경계에서 예외는 '실패' 가 아니라 '미확인' 이다.
