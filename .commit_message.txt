📝 docs: AGENTS.md 신설(CLAUDE.md 쌍둥이) + 문서의 죽은 사실 3건 교정 — ①**AGENTS.md 가 없었다.** Codex 등 다른 에이전트가 이 저장소에서 아무 지침 없이 작업하고 있었다(main 에는 있다). CLAUDE.md 를 헤더만 바꿔 생성했고, **두 파일이 쌍둥이라는 사실을 양쪽 머리에 명시**했다 — 오늘 main 에서 AGENTS.md 만 갱신되고 CLAUDE.md 가 stale 로 남는 divergence 가 실제로 발생했다. ②**Vercel 스코프가 죽은 이름이었다**: 문서의 `goodman-jways` 는 개명돼 지금은 `The specified scope does not exist` 로 실패한다(실측 확인). 4곳을 `goodman-ksways` 로 고치고 개명 사실을 남겼다 — 문서대로 따라 하면 배포 명령이 그냥 실패하던 상태였다. ③**테스트 수치가 120 여 건 어긋나 있었다**(문서 34 files/322 tests, 실제 39/448). 개수를 빼고 실행 명령으로 대체하면서 **개수를 문서에 적지 말라는 규칙**을 명시했다. ⚠️main 문서의 `test:coverage` 를 그대로 복사하지 않았다 — 이 저장소엔 그 스크립트가 없어서 없는 명령을 문서화할 뻔했다(package.json 확인 후 `test:e2e` 만 기재). 문서만 변경, 코드·동작 변경 없음.
