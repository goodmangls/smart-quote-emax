🌍 카피(마케팅) 목적지 국가 수 190 → 250+ 로 정정, main 과 동일 정책 적용 — **두 사이트가 서로 다른 숫자를 광고하고 있었다.** smart-quote-main 은 이미 `250+` 이고 `copy.md` 에 정책까지 명문화돼 있었는데(UPS·DHL·FedEx 합산 목적지는 최대 약 265개국이나 과장·세부 변동을 피해 대외 카피는 `250+` 로 통일), emax 만 `190` 에 머물러 있었다. 같은 캐리어 조합을 쓰는 형제 저장소라 숫자가 갈리면 그 자체가 신뢰 문제다. 코드 8곳 + 문서 3곳 정정: `index.html` 메타 3종(description·og·twitter — SEO 노출 문구라 실제로 검색결과에 찍힌다) · `src/i18n/translations.ts` KO 3곳(`landing.title.main`·히어로 설명·`landing.globalPorts`) · EN 2곳(`landing.title.sub`·`landing.globalPorts`) · `src/pages/LandingPage.tsx` 통계 타일 2곳(히어로/하단 각 1). ⚠️**`landing.stat.countries` 라벨에서는 숫자를 뺐다**(`190개 목적지 국가`→`목적지 국가`, `190 Destinations`→`Destinations`). 타일이 `value` 로 이미 `250+` 를 렌더하므로 라벨에도 숫자가 있으면 **`250+` 위에 `250개 목적지 국가` 가 겹쳐 보이고 `+` 유무까지 어긋난다.** main 이 이미 라벨을 숫자 없이 두고 있어 구조도 함께 맞춘 것이다 — 단순 숫자 치환만 했다면 이 중복이 그대로 남았다. 📝 `copy.md` 에 **표기 정책을 옮겨 적었다**: 숫자는 타일 `value` 한 곳에만 두고, 대외 카피는 `250+` 로 통일하며, 정확한 개수·ISO 목록의 기준은 언제나 요율표라는 것. main 에만 있던 정책이라 emax 가 뒤처진 것이므로 재발 방지의 핵심이 이 문단이다. 치환은 **파일별 등장 횟수를 단언하는 스크립트**로 수행해 누락·과잉을 막았고(요율표의 `190200`·`1900` 같은 금액과 섞이지 않도록 `_tariff.ts`·`public/data` 제외), 사후에 `190` 잔존 0건을 재확인했다. ⚠️**소스가 아니라 빌드 산출물로 검증**: `dist/index.html` 메타가 `전 세계 250+개국`, 번들에 `250+개국 국제 운임`·`250+ Countries, Instantly.` 가 있고 `190개국`·`190+ Countries`·`190 Destinations` 는 0건. 검증: tsc 0, eslint 0, vitest 40파일 472건, build 성공(청크 경고는 기존건). 테스트·e2e 가 이 문구에 의존하지 않음을 사전 확인했다(지역화 텍스트 셀렉터 금지 원칙대로 셀렉터는 href/role/testid 기반).
