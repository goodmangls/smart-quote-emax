📝 CLAUDE.md·AGENTS.md 에 "개수를 적지 않는다"·쌍둥이 동기화 규약 추가 (번역 키 ~800 → 실제 723 정정) — 이 저장소의 쌍둥이는 **정상이었다**(1·3·5행 외 차이 0줄). 다만 smart-quote-main 쪽이 갈라져 있었고, 같은 일이 여기서도 반복될 수 있어 **검사법을 문서에 박았다**: `diff <(tail -n +6 CLAUDE.md) <(tail -n +6 AGENTS.md)` 가 비어야 정상. 🔴**썩은 수치 하나를 제거했다** — `i18n/translations.ts` 설명에 **"~800 keys"** 라고 적혀 있는데 실측 **약 723개**다(10% 어긋남). 숫자를 723으로 고치면 다음 기능 추가에 또 썩으므로 **수치를 지우고 파일을 가리키게** 바꿨다. 이 저장소 문서는 이미 FSC 요율과 테스트 개수에 같은 방식을 쓰고 있었는데(실제로 2026-07-20 FSC 값이 8월 말까지 잔존했고, 테스트 수는 2026-08 기준 120여 건 어긋나 있었다) 원칙이 세 군데에 흩어져 있어 **「문서에 개수를 적지 않는다」 절로 통합**하고 무엇이 언제 썩었는지 이력 표를 남겼다. 남겨도 되는 숫자는 **코드가 강제하는 정책값**(존 이름, FedEx 18kg 최소청구중량, 요율표 all-or-nothing)뿐이라고 명시했다. ⚠️**main 의 수치를 이 문서로 옮기지 말라**는 경고도 함께 넣었다 — 두 저장소는 UPS 서류 요율부터 다르고(2026-09-08 사용자 확인) 맞추면 견적이 26% 뛴다. 이건 이미 요율 항목에 적혀 있던 사실이지만, "개수" 규약 자리에서 다시 만나는 함정이라 그 자리에도 뒀다. ✅**AGENTS.md 는 손으로 고치지 않고 CLAUDE.md 에서 생성했다** — 1·3·5행(제목·대상 독자·상호참조)만 치환하는 스크립트를 써서 드리프트가 원리적으로 불가능하게 했고, 생성 직후 **6행 이후 완전 일치**를 프로그램으로 재확인했다(전체 차이 6줄 = 의도된 3쌍). ⚠️이 저장소에는 `docs/superpowers/plans/` 가 아예 없어서 오늘 main·bridgelogis·ASCA 에 넣은 "계획 체크박스는 상태가 아니다" 규약은 **해당 사항이 없다** — 없는 디렉토리에 대한 규칙을 넣지 않았다. 변경은 `CLAUDE.md`·`AGENTS.md` 2개뿐이고 **코드 변경 0건**임을 `git diff --name-only` 로 확인했다.
